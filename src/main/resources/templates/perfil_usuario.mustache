<!DOCTYPE html>

<html lang="es">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Perfil de Usuario</title>
<script src="https://cdn.tailwindcss.com"></script>
<style>
    body { font-family: 'Inter', sans-serif; }
</style>
</head>
<body class="bg-gray-100 flex items-center justify-center min-h-screen p-4">

    <div class="bg-white p-8 rounded-lg shadow-2xl w-full max-w-2xl transform transition duration-500 hover:shadow-xl">
        <h1 class="text-3xl font-bold text-center text-gray-800 mb-6">Mi Perfil</h1>

        <div class="space-y-4 mb-8 p-6 bg-blue-50 border border-blue-200 rounded-xl">
            <div class="flex justify-between border-b border-blue-200 pb-2">
                <span class="font-semibold text-gray-700">Nombre de Usuario:</span>
                <span class="font-extrabold text-blue-700">{{username}}</span>
            </div>
            <div class="flex justify-between">
                <span class="font-semibold text-gray-700">ID de Cuenta:</span>
                <span class="text-gray-800 font-mono text-sm bg-blue-100 p-1 rounded-md">{{userId}}</span>
            </div>
        </div>
        
        <div class="text-center mt-6">
            <a href="/dashboard" class="inline-block bg-gray-600 text-white py-2 px-6 rounded-lg hover:bg-gray-700 transition duration-300 text-lg shadow-md">
                Volver al Panel
            </a>
        </div>
    </div>

</body>
</html>